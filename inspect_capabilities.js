const TrackPlayer = require('react-native-track-player');
const { Capability } = require('react-native-track-player');

console.log('Capabilities:', Object.keys(Capability));
console.log('Events:', Object.keys(TrackPlayer.Event));
